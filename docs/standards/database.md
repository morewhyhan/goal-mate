# Database Standard

## 表结构

- 表名、字段名使用稳定命名，避免业务含义模糊。
- 每张业务表必须有主键、创建时间、更新时间。
- 关键唯一性规则必须使用唯一索引表达。
- 高频查询路径必须设计索引，并说明查询场景。

## 迁移

- 数据库变更必须可重复执行或有明确迁移脚本。
- 字段删除和数据回填必须写明回滚策略。
- DDL 事实同步到 `docs/designs/database.sql` 或对应迁移目录说明。
