# API Standard

## 契约

- API 必须在 OpenAPI 3.x 中定义请求、响应、错误结构和鉴权要求。
- 禁止只写自然语言接口说明。
- 字段命名、类型、必填规则必须与领域模型和数据库设计一致。

## 响应

- 成功响应返回明确资源或操作结果。
- 失败响应包含稳定错误码、用户可读消息和调试追踪 ID。
- 分页接口必须定义 `page`、`pageSize`、`total` 或等价字段。

## 兼容性

- 删除字段、修改字段含义、修改错误码视为破坏性变更，必须进入 Plan 和 ADR。
- 新字段默认保持向后兼容。
