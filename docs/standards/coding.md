# Coding Standard

## 通用规则

- 代码优先表达业务意图，避免过度抽象。
- 函数保持单一职责，复杂分支需要拆分或补充测试。
- 命名使用领域语言，不使用无意义缩写。
- 禁止把临时调试代码、密钥、个人路径提交到仓库。
- 错误处理必须给出明确上下文，不吞异常。

## AI 生成代码要求

- 不生成未使用代码、死代码或占位逻辑。
- 不引入未确认的新依赖。
- 不为了通过测试硬编码业务结果。
- 修改公共接口时同步更新调用方、文档和测试。
