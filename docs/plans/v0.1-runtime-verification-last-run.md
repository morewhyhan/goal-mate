# Goal Mate v0.1 Local Runtime Verification

Date: 2026-07-02

Scope: local WSL runtime, Next dev server on `127.0.0.1:3000`, SQLite dev database.

## Result

Local runtime verification passed.

This does not prove server long-running operation, QQ Gateway stability, Scheduler delivery, or browser visual quality.

## Commands and evidence

| Check | Result | Evidence |
| --- | --- | --- |
| Static gate | PASS | `pnpm verify:static` passed: secret scan and deployment config checks passed |
| Prisma Client | PASS | `pnpm db:generate` generated Prisma Client |
| TypeScript | PASS | `pnpm typecheck` exited 0 |
| Existing DB migration | PASS | `pnpm exec prisma migrate deploy` applied `20260702000500_agent_tools_scheduler` |
| Seed | PASS | `pnpm db:seed` printed `Seeded Goal Mate demo data for demo@goalmate.local` |
| Unauthenticated API smoke | PASS | `pnpm verify:v01:api` returned PASS for health and 401 security checks |
| Authenticated API smoke | PASS | Temporary user with session cookie passed all authenticated API smoke checks |
| Business flow | PASS | Temporary user with seed data passed `pnpm verify:v01:business` |
| Agent Loop write flow | PASS | Temporary user with seed data passed `pnpm verify:agent-loop:write` |
| Production build | PASS | `pnpm build` compiled and generated all dashboard routes |
| Dashboard page HTTP smoke | PASS | `/dashboard/today`, `/dashboard/goals`, `/dashboard/logs`, `/dashboard/agent`, `/dashboard/settings` all returned HTTP 200 |
| Dashboard visual smoke | PASS with fix | Edge headless screenshots inspected for Today, Goals, Logs, Agent and Settings |
| Dashboard browser smoke | PASS | `pnpm verify:dashboard-browser:write` opened Today, Goals, Logs, Agent and Settings in Edge headless; checked required text, horizontal overflow, Today heatmap cells, Goals read-only structure, Logs editor visibility, Agent fixed input and Settings controls |
| Authenticated dashboard browser smoke | PASS | `pnpm verify:dashboard-browser:auth:write` created or signed in a local browser user, seeded real Goal Mate data, signed in through the real browser auth flow, and verified Today, Goals, Logs, Agent and Settings with screenshots |
| Final production build after visual fix | PASS | `pnpm build` completed after heatmap layout correction |
| Runtime worker syntax gate | PASS with fix | `pnpm verify:static` now checks `qq-bot-worker.mjs` and `scheduler-worker.mjs` with `node --check` |
| Scheduler one-shot verification | PASS | Temporary reminder rule plus `pnpm worker:scheduler:once` created a `SchedulerEvent` with `status=failed` and `errorMessage=No enabled QQ binding.` |
| Local self-hosted deployment config | PASS | `.env.example`, systemd README, Web service env, deployment tests and report templates now include production URL variables and one-shot Scheduler verification |
| Local deployment bundle | PASS | `pnpm deploy:bundle` creates a local `.artifacts/deploy/goal-mate-*.tar.gz` package and refuses to include real `.env`, Git history, dependencies, build output, local DB files or logs |

## Issues found and fixed

| Issue | Fix |
| --- | --- |
| Logs API typecheck failed because `frontmatter` could be `null` | Normalized `frontmatter` to `Record<string, unknown> | undefined` before calling Markdown store |
| `/api/settings/export` returned 500 on DBs missing new runtime tables | Applied existing migration `20260702000500_agent_tools_scheduler` |
| Agent Loop verifier could not read source files from `src/package.json` working directory | Resolved verifier file reads relative to the script app root |
| Today heatmap looked like it had a horizontal scrollbar inside the panel | Reworked heatmap cells to fit the card and removed the confusing legend strip |
| Layout regressions were only manually/screenshot checked | Added `pnpm verify:dashboard-browser` so the key page layout risks are checked by browser automation and screenshots are written to `.artifacts/browser-smoke/` |
| Browser smoke only covered empty state | Added `pnpm verify:dashboard-browser:auth` so real seeded goals, KR, logs, Agent history and Settings are checked in the browser without writing cookies to reports |
| QQ Worker contained a stale duplicated tool-executor fragment and failed Node syntax check | Removed the stale fragment and added worker syntax checks to deployment verification |
| Scheduler required waiting for a real clock minute to verify reminders | Added `--once --force-reminder=morning_planning` and `pnpm worker:scheduler:once` |
| Self-hosted docs missed production Web URL variables and server user setup | Added `BETTER_AUTH_URL`, `NEXT_PUBLIC_BETTER_AUTH_URL`, `NEXT_PUBLIC_APP_URL`, `HOSTNAME`, `PORT`, `goalmate` user setup, migrate/build/static-gate steps |
| Local deployment preparation had no safe package boundary | Added `pnpm deploy:bundle` and deployment checks so local packaging is explicit, reproducible and secret-safe before any server upload |

## 2026-07-03 implementation facts after this run

| Item | Fact |
| --- | --- |
| Auth UI scope | Added the v0.1 Login / Register / Session requirement domain as F13 |
| Login entry | Root now enters the login flow instead of directly opening Dashboard |
| Dashboard guard | Dashboard private pages are gated by client session state before rendering children |
| Logout | Sidebar exposes the current user and a logout action |
| Fake actions | Unimplemented third-party login and forgot-password actions were removed from the old auth modal |
| Verification | 2026-07-03 rerun key checks: `pnpm typecheck`, `pnpm build`, `pnpm verify:auth-ui:write`, `pnpm verify:dashboard-browser:auth:write`; final results passed |

## 2026-07-03 auth UI verification

| Check | Result | Evidence |
| --- | --- | --- |
| TypeScript | PASS | `pnpm typecheck` exited 0 |
| Production build | PASS | `pnpm build` generated `/login` and all Dashboard routes |
| Auth UI smoke | PASS after script origin fix | `pnpm verify:auth-ui:write` passed Login shell, unauthenticated Dashboard guard, UI register, logout and UI login |
| Auth isolation smoke | PASS | `pnpm verify:auth-isolation:write` passed two-user goal/log/thread/model/export isolation and temporary data cleanup |
| Authenticated Dashboard browser smoke | PASS after stable Today assertion fix | `pnpm verify:dashboard-browser:auth:write` passed Today, Goals, Logs, Agent and Settings |

Issues found and fixed during 2026-07-03 auth verification:

| Issue | Fix |
| --- | --- |
| `verify:auth-ui` initially used `127.0.0.1`, while Auth Client used `localhost`; browser treated auth calls as cross-origin credential requests | Changed `verify-auth-ui.mjs` default base URL to `localhost` / app auth URL so UI auth runs same-origin |
| `verify:auth-ui` once failed during repeated login because the script filled inputs before React finished switching forms | Added selector waiting and explicit missing-input errors before filling login/register fields |
| Dashboard browser smoke asserted unstable seed text for Today | Changed Today authenticated assertion to stable page structure: current focus, done-when, minimum, fallback and logout |

## 2026-07-03 user model secret isolation update

Current implementation facts:

| Item | Fact |
| --- | --- |
| Model API Key ownership | Model API Key is now user-owned settings data, not a shared server-level default |
| Storage | User-entered API Key is encrypted into `ModelConfig.apiKeyRef` |
| Response boundary | `/api/models`, Settings control center, Settings export and Agent tool model read return only masked status |
| Runtime usage | Web Agent, QQ Worker, Scheduler Worker and Intervention Planner resolve the current user's ModelConfig by userId |
| Server env | `DEEPSEEK_API_KEY` is no longer a required deployment variable for normal product usage; it remains only an optional compatibility/live-test path |
| Verification asset | `pnpm verify:auth-isolation` now checks two-user model key isolation, encrypted at-rest storage and export redaction |

Verification run:

| Check | Result | Evidence |
| --- | --- | --- |
| TypeScript | PASS | `pnpm typecheck` exited 0 after user model secret changes |
| Static gate | PASS | `pnpm verify:static` passed secret hygiene and deployment config checks |
| Auth isolation with model secrets | PASS | `pnpm verify:auth-isolation:write` passed A/B user API Key save, encrypted at-rest refs, response redaction, export redaction and cross-user blocking |
| Authenticated Dashboard browser smoke | PASS | `pnpm verify:dashboard-browser:auth:write` passed Today, Goals, Logs, Agent and Settings after Settings API Key UI change |
| Production build | PASS | `pnpm build` exited 0 and generated `/dashboard/settings` and all existing routes |

Note: `next build` was run while the local `next dev` server was still running, so the dev server printed transient `.next` hot-reload errors when stopped. The build command itself exited 0, and the browser smoke had already passed before the build.

## Remaining unproven items

- Complete subjective manual UX review beyond automated browser smoke.
- Server long-running Web / QQ Worker / Scheduler Worker operation.
- QQ Gateway stability and actual platform send behavior.
- Scheduler active reminder delivery and reply loop while the local computer is off.
