# Historical: Goal Mate v0.1 验收报告

Status: historical run record, not current HEAD truth.

This file records a previous local acceptance run on 2026-07-02. The project has changed after that run, so this document must not be used to claim that current HEAD is verified.

Current verification truth is maintained in:

```text
docs/plans/verification-overview.md
```

## 1. 执行信息

| 项 | 内容 |
| --- | --- |
| 执行时间 | 2026-07-02 |
| 执行环境 | local WSL, Next dev server `http://127.0.0.1:3000` |
| 数据库策略 | reset |
| Seed 状态 | 已执行 |
| 验收结论 | 历史通过记录；不代表当前 HEAD |

## 2. 命令执行结果

| 命令 | 结果 | 关键输出 |
| --- | --- | --- |
| `pnpm db:generate` | 通过 | Prisma Client generated |
| `pnpm db:reset` | 通过 | 应用 `20260702000100_goal_mate_v01` |
| `pnpm db:seed` | 通过 | `Seeded Goal Mate demo data for demo@goalmate.local` |
| `pnpm verify:v01:prepare-auth` | 通过 | `goals=1, logs=1, threads=1` |
| `pnpm typecheck` | 通过 | `tsc --noEmit` 无错误 |
| `pnpm build` | 通过 | `/dashboard/today`、`/dashboard/goals`、`/dashboard/logs`、`/dashboard/agent`、`/dashboard/settings` 均生成 |
| `pnpm verify:v01:api:auth:write` | 通过 | API smoke 全部 PASS |
| `pnpm verify:v01:business:write` | 通过 | Business flow 全部 PASS |

## 3. PRD 验收项

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| V01-AC-1 Agent 能承载目标沟通上下文 | 通过 | `BF-AGENT` PASS，页面 `/dashboard/agent` 200 |
| V01-AC-2 系统能生成/承载目标推理卡 | 通过 | `BF-GOAL` PASS，KR=4、conditions=5、stages=1 |
| V01-AC-3 Today 只展示当前最重要一步 | 通过 | `BF-TODAY` PASS，页面 `/dashboard/today` 200 |
| V01-AC-4 Goals 只读展示目标结构 | 通过 | `BF-GOAL` PASS，页面 `/dashboard/goals` 200 |
| V01-AC-5 Logs 层级和 Markdown 编辑 | 通过 | `BF-LOGS` PASS，页面 `/dashboard/logs` 200 |
| V01-AC-6 Agent 历史、消息区、固定输入 | 通过 | `BF-AGENT` PASS，页面 `/dashboard/agent` 200 |
| V01-AC-7 未完成时能诊断原因 | 通过 | `BF-CHECKIN` PASS，partial checkin 创建 Diagnosis |
| V01-AC-8 Settings 能配置模型、提醒、日志和隐私 | 通过 | `BF-SETTINGS-WRITE`、`BF-MODEL-WRITE`、`BF-MODEL-TEST` PASS |
| V01-AC-9 复盘写入 Markdown 日志 | 通过 | `BF-REVIEW` PASS，写入周志 Markdown |

## 4. 页面验收

| 页面 | 路由 | 结果 | 证据 |
| --- | --- | --- | --- |
| Today | `/dashboard/today` | 通过 | HTTP 200 |
| Goals | `/dashboard/goals` | 通过 | HTTP 200 |
| Logs | `/dashboard/logs` | 通过 | HTTP 200 |
| Agent | `/dashboard/agent` | 通过 | HTTP 200 |
| Settings | `/dashboard/settings` | 通过 | HTTP 200 |

## 5. 缺陷列表

| 编号 | 严重级别 | 来源 | 现象 | 处理 | 状态 |
| --- | --- | --- | --- | --- | --- |
| BUG-v01-001 | Medium | 验收过程 | 旧迁移仍为 Ignite 模板，seed 找不到 Goal Mate 表 | 新增 `20260702000100_goal_mate_v01` 迁移 | 已解决 |
| BUG-v01-002 | Medium | 验收过程 | 无默认 `DATABASE_URL` 时 Prisma reset 失败 | 新增 `src/.env` 和 `src/.env.example` | 已解决 |
| BUG-v01-003 | Medium | 验收过程 | 登录态验收没有可复现 cookie 和 seed 数据绑定方式 | 新增 `prepare-v01-auth-session.mjs` 和 `verify:v01:prepare-auth` | 已解决 |
| BUG-v01-004 | Low | 验收过程 | `next build` 与运行中的 `next dev` 并行会污染 dev server 的 `.next` 运行态 | 记录 runbook 注意事项；运行时验收前重启 dev server | 已规避 |

## 6. 最终判断

该次本地运行曾通过当时的验收脚本，但不能代表当前 HEAD。当前代码需要重新执行静态、类型、API、页面和服务器长期运行验收后，才能重新声明通过。
