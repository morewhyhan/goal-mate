-- 当前数据库设计占位。
-- 新增业务表时，请同步字段、索引、约束和必要注释。

-- Example:
-- CREATE TABLE example_entities (
--   id VARCHAR(64) PRIMARY KEY,
--   created_at TIMESTAMP NOT NULL,
--   updated_at TIMESTAMP NOT NULL
-- );
