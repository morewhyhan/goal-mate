# Feature: <需求名称>

## 1. 需求类型

- 类型：增量需求 / 存量修改
- 模块：<模块名>
- 负责人：<产品/BA/研发>
- 日期：<YYYY-MM-DD>

## 2. 业务背景

说明用户、场景、业务目标和当前问题。

## 3. 模块范围

### 3.1 如果是增量需求，填写本节

- 新增模块：<模块名>
- 用户角色：<角色>
- 入口：<页面/API/菜单/任务入口>
- 出口：<完成后的结果或跳转>
- 主流程：<从开始到完成的核心步骤>
- 涉及能力：<页面/API/数据库/权限/消息/文件等>

### 3.2 如果是存量修改，填写本节

- 现有模块：<模块名>
- 当前行为：<现在系统如何工作>
- 目标行为：<修改后应该如何工作>
- 变更清单：

| 类型 | 对象 | 当前状态 | 目标状态 | 兼容性影响 |
| --- | --- | --- | --- | --- |
| 新增/修改/删除/废弃 | 页面/接口/字段/规则 | <当前> | <目标> | <无/有，说明> |

## 4. 原型与设计

- 原型类型：Figma / Stitch / HTML / 无
- 原型链接或路径：<链接或仓库路径>
- 页面/模块映射：<原型 Frame 与需求模块的对应关系>

## 5. 字段清单

增量需求：列出完整字段。

存量修改：只列受影响字段，但必须说明与现有字段的关系。

| 字段名 | 类型 | 必填 | 默认值 | 数据来源 | UI 展示 | 校验规则 | 变更类型 | 描述 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| id | string | 是 | - | 系统生成 | 否 | 唯一 | 新增/修改/删除/不变 | 资源 ID |

## 6. 业务规则

每条规则必须独立、可测试、无歧义。

- R1：<规则>
- R2：<规则>

存量修改需要额外标注规则变化：

| 规则编号 | 当前规则 | 目标规则 | 影响范围 |
| --- | --- | --- | --- |
| R1 | <当前> | <目标> | <页面/API/数据/测试> |

## 7. 验收标准

- AC1：Given <前置条件> When <操作> Then <结果>。
- AC2：Given <前置条件> When <操作> Then <结果>。

存量修改必须至少包含：

- 变更后的新行为验收。
- 不应被破坏的旧行为验收。
- 兼容性或数据迁移验收，如适用。

## 8. 非目标

- 本轮不包含：<明确排除项>

## 9. 待确认问题

- Q1：<需要业务确认的问题>
